
@FunctionalInterface //functional interface annotation, ensures that it can only contain one function only.
public interface Greeting {
    
    // Printing a greeting message to the console. 
    void sayHello(); 
    
}
